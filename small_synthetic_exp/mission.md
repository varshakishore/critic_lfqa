I want to test what kinds of behaviors DPO can learn. I want to test whether DPO can learn to use (*) as list markers. Come up with 2000 prompts that require models to generate a list (the outputs should be at most 512 tokens). Create DPO pairs such that the marker used is replaced with (*). Run DPO to finetune a qwen3-4b model and evaluate the model. Use DPOTrainer from TRL to run DPO. 